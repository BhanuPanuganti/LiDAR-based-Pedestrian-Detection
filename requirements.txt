# Core ML & data
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
xgboost>=1.6
scipy>=1.7
tqdm>=4.62

# Visualisation
matplotlib>=3.5
seaborn>=0.11
open3d>=0.15        # optional: 3D point cloud viewer

# PyTorch — install matching your CUDA version:
# CUDA 12.1 (RTX 4050):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# OpenPCDet — install separately from source:
#   git clone https://github.com/open-mmlab/OpenPCDet.git
#   cd OpenPCDet && pip install -r requirements.txt && python setup.py develop
